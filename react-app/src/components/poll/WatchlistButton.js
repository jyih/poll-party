import React, { useEffect, useState } from 'react';
import { useSelector, useDispatch } from 'react-redux';

const WatchlistButton = ({ poll_id }) => {
  const user = useSelector(state => state.session.user);

  return (
    <>
      
    </>
  );
}

export default WatchlistButton;